spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/judge_db?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: c!!2211paKev
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect

    hibernate:
      ddl-auto: update

    open-in-view: false

    properties:
      hibernate:
      format_sql: true


